import { styled, Box } from '@mui/material';

export const SaleEndsBadge = styled(Box)`
    background: var(--color-warning);
    border-radius: 8px;
    padding: 4px 8px;
    color: white;
    display: inline-block;
`;
