import { styled } from '@mui/material';
import { SmallTypography } from 'src/core/typographies';

export const Unread = styled(SmallTypography)`
    border-radius: 8px;
    color: var(--color-base);
    border: 1px solid var(--color-base);
    padding-left: 0.5rem;
    padding-right: 0.5rem;
    margin-left: 0.5rem;
`;
