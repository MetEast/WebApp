import { styled, Button } from '@mui/material';

export const FilterButton = styled(Button)`
    background: #e8f4ff;
    padding: 8px 20px;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 700;
    color: #1890ff;
`;
