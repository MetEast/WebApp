import { styled, Box } from '@mui/material';

export const Container = styled(Box)`
    width: fit-content;
    background: #ffead8;
    color: #e0822c;
    border-radius: 8px;
    padding: 6px 8px;
    font-size: 14px;
    font-weight: 500;
`;
