import { styled, Box, Button } from '@mui/material';

export const SortByBtn = styled(Button)`
    display: flex;
    flex-direction: row;
    background: #e8f4ff;
    padding: 8px 20px;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 700;
    color: #1890ff;
    img {
        width: 20px;
        height: 20px;
        margin-right: 6px;
    }
`;
