import React from 'react';
import { Box, Stack, Typography } from '@mui/material';

const RewardsPage: React.FC = (): JSX.Element => {
    return <Stack>RewardsPage</Stack>;
};

export default RewardsPage;
